# travel-friend

## Travel Friend — Telegram Mini App для поиска попутчиков и совместного планирования путешествий с помощью ИИ

Проект помогает людям находить подходящую компанию для поездки, знакомиться в чате, обсуждать идеи путешествия и превращать разрозненные желания участников в понятный маршрут, бюджет и план действий.

На этапе MVP продукт реализуется не как отдельное мобильное приложение, а как Telegram Mini App. Это позволяет быстрее проверить гипотезу, упростить вход для пользователей и сократить время разработки.

## Простое объяснение

Если коротко, Travel Friend нужен для того, чтобы человеку было проще:

1. понять, с кем ему комфортно поехать;
2. начать общение без лишних барьеров;
3. договориться о поездке;
4. получить помощь ИИ прямо в процессе обсуждения;
5. собрать из хаотичных пожеланий понятный план поездки.

Для нетехнических участников команды это можно описать так: пользователь открывает Telegram Mini App, заполняет профиль и travel-анкеты, смотрит подходящих людей, пишет им в чат и вместе с ИИ обсуждает будущую поездку.

## Почему Telegram Mini App

На текущем этапе Telegram Mini App выбран как основная форма MVP, потому что:

1. пользователю не нужно ставить отдельное приложение;
2. проще быстро показать работающий продукт;
3. вход для первых пользователей короче и понятнее;
4. проще тестировать гипотезу в реальном Telegram-сценарии;
5. часть пользовательского пути уже встроена в сам Telegram.

Telegram Mini App не отменяет будущую отдельную мобильную версию. Если MVP подтвердит спрос, текущую продуктовую логику можно будет переиспользовать дальше.

## Что уже есть сейчас

Текущий проект уже работает как настоящий Telegram Mini App через Vercel, а не как локальный прототип.

Уже сделано в `develop`:

- каркас Next.js Mini App в `apps/mini-app`
- нижняя мобильная навигация
- маршруты для `/`, `/chats`, `/trips` и `/profile`
- определение окружения Telegram SDK
- резервный режим браузера для локальной разработки вне Telegram
- исправление несовпадения состояния при гидратации Telegram state
- публичный запуск через Vercel как основной способ проверки Mini App в Telegram
- автоматическая настройка Telegram Menu Button через инженерный сценарий
- отдельная Debug Telegram Page для диагностики окружения и WebApp-поведения
- server-side Telegram Auth: raw `initData` проверяется FastAPI, а backend выдаёт opaque Bearer session; UI проходит onboarding или открывает основной интерфейс по его результату
- Profile и active TravelIntent: read/write API текущего пользователя и frontend hydration/edit flow
- Discover: backend candidates, финальные `interested` / `rejected` решения и reciprocal `Match → direct Chat`
- Chats: persisted direct и group список, server-side message history и отправка текстовых сообщений
- Group Chats: создание из existing matched/direct-chat companions, минимум три участника и фиксированный MVP-состав
- Trips: persistence, server API создания из direct/group Chat, `trip_stops`, backend Trips List и read-only Trip Detail
- публичные профили попутчиков на маршруте `/buddies/[id]`
- сохраняются historical mock/screens как UI fallback и дизайн-ориентир там, где backend contract ещё не реализован
- единый порядок категорий и текстовые состояния категорий
- двусторонняя навигация Chat ↔ Trip: Chat Room ведёт в конкретный связанный Trip, а Trip Details ведёт в конкретный Chat
- Trip создаётся только из существующего direct или group Chat; все активные ChatParticipant сразу становятся TripParticipant
- в Chat Room скрыта Bottom Navigation, а длинные и многострочные сообщения уже исправлены
- проверка Chat Room выполнена локально, через Vercel Preview и внутри Telegram Mini App
- единый frontend data model слой для профилей, travel preferences, чатов и решений интереса
- обновлённая нижняя навигация с основной группой Chats / Discover / Trips и отдельным Profile

Рабочий MVP flow backend-backed: direct и group Chat, persisted messages, создание Trip из Chat и Trips List/Detail читают persistence. Mini App создаёт Trip через API; при `409` читает `GET /trips` и открывает существующую незавершённую Trip того же Chat. Realtime, read receipts, pagination, Trip write/lifecycle, TripInvitation, сложный membership lifecycle, AI/Proposal и provider integrations также отсутствуют. Логическая ER-модель остаётся design-источником для deferred архитектуры и не должна приниматься за перечень уже поставленных таблиц/API.

### Почему мы отказались от ngrok

Раньше временный туннель был удобен только на коротком отрезке времени, но для ежедневной разработки он оказался слабым местом:

- URL мог меняться, а Telegram Mini App лучше работает с постоянным адресом;
- сложнее стабильно повторять один и тот же сценарий проверки;
- публичный Vercel URL ближе к реальному продакшен-поведению;
- команде проще опираться на один стабильный адрес, чем каждый день перенастраивать туннель.

Теперь Vercel используется как основной внешний контур для Mini App, а локальная разработка остаётся для быстрой итерации внутри `develop`.

### Как сейчас устроена разработка

Основной рабочий поток теперь выглядит так:

1. `main` хранит проверенное состояние.
2. `develop` собирает текущую интеграцию и ежедневную работу.
3. Для каждой задачи создаётся отдельная ветка `feature/*`.
4. Задача реализуется в своей ветке, после чего она вливается в `develop`.
5. После проверки и согласования `develop` мержится в `main`.

Это позволяет не смешивать эксперимент, диагностику и готовый результат в одной ветке и проще возвращаться к стабильному состоянию.

### Debug Telegram Page

`Debug Telegram Page` используется как инженерный инструмент, а не как пользовательская функция.

Она нужна для того, чтобы:

- быстро проверять состояние Telegram WebApp в конкретном окружении;
- отделять проблемы приложения от проблем Telegram-контекста;
- воспроизводить интеграционные ошибки без догадок;
- сравнивать поведение локального браузера, Telegram WebView и Vercel.

Важно: AI-помощник не выделен в отдельную вкладку. Его интеграция в Chats/Trips отложена до подключения второго разработчика.

## Текущий статус проекта

Сейчас проект можно описать так:

- завершён первый инфраструктурный этап проекта;
- `develop` успешно объединён в `main` как первый стабильный checkpoint;
- команда уже перешла от настройки инфраструктуры к первым пользовательским MVP-сценариям;
- Telegram Mini App уже разворачивается и проверяется через Vercel;
- базовая навигация и маршрутизация закреплены;
- интеграция Telegram WebApp проверяется в реальном окружении, а не только локально;
- есть инженерный контур для диагностики проблем;
- появились первые фокусные экраны и потоки: Profile, Discover, public buddy profile, interest/match и Chats list;
- сохранён frontend mock/local-state checkpoint как UI-основа для незакрытых сценариев;
- логическая доменная модель backend завершена и согласована;
- утверждённая ER-модель закреплена как источник истины для дальнейших backend-этапов;
- настроен local Supabase CLI workflow и local PostgreSQL/Supabase environment;
- завершён Core Identity persistence: `users`, `telegram_identities`, `profiles`, `profile_photos`, `user_settings`, `user_activity_states` и `travel_intents` с нужными constraints и indexes;
- завершён server-side Telegram Auth: проверка raw `initData`, `POST /auth/telegram`, `POST /auth/logout`, Bearer authentication и server-side multiple sessions с opaque token, SHA-256 hash в БД и TTL 30 дней;
- первый login создаёт базовые User/TelegramIdentity/UserSettings/UserActivityState, повторный использует того же User; revoked, expired и deleted-user sessions отклоняются;
- `DATABASE_URL` читается и валидируется только в server environment;
- реализованы Profile + TravelIntent API, onboarding, persistence Discover interest/match, direct/group Chats/Messages и Trip slice с `trip_stops`;
- Group Chat создаётся с минимум тремя участниками: создатель выбирает existing matched/direct-chat companions, а состав фиксирован для MVP;
- Trip создаётся из direct или group Chat; все активные ChatParticipant сразу становятся TripParticipant; TripInvitation не используется; Mini App вызывает API и при `409` открывает существующую unfinished Trip через `GET /trips`;
- realtime, Trip write/lifecycle, invitations, сложный membership lifecycle, RLS/production backend deployment и AI/Proposal остаются дальнейшими отдельными задачами;
- AI/Proposal и provider integrations отложены до подключения второго разработчика;
- это уже не черновой каркас, а рабочая основа для MVP с постоянным URL, понятным процессом разработки и синхронизированной документацией.

## Идея проекта

У молодых путешественников часто есть желание поехать куда-то, но нет подходящей компании, большого бюджета или опыта самостоятельного планирования. Из-за этого поездка откладывается, становится дороже или вовсе не происходит.

Travel Friend решает эту проблему через сочетание двух механик:

1. Поиск совместимых попутчиков. Пользователь заполняет профиль, указывает интересы, бюджет, стиль отдыха и предпочтения, после чего может искать людей для совместной поездки.
2. AI Travel Copilot как будущий слой поверх Chats / Trips. Позже ИИ поможет участникам договориться: будет анализировать желания группы, предлагать направления, маршруты, примерный бюджет, жильё, транспорт и активности.

## Для кого продукт

Основная целевая аудитория на старте:

- зумеры и молодые путешественники;
- студенты и молодые специалисты;
- люди, которым не хватает компании для поездок;
- пользователи с ограниченным бюджетом;
- люди, которым хочется путешествовать интереснее, дешевле и безопаснее.

При этом продукт может быть полезен и более широкой аудитории: как сервис поиска компании и как AI-помощник для самостоятельного планирования путешествий.

## Главная ценность

Travel Friend не является просто планировщиком путешествий и не является просто приложением знакомств.

Главная ценность проекта — помочь пользователю:

- найти людей с похожими интересами и бюджетом;
- начать обсуждение будущей поездки;
- согласовать желания участников;
- получить понятный план путешествия;
- снизить стоимость поездки за счёт совместного участия;
- сделать путешествие более интересным за счёт компании и геймификации.

## MVP

Цель MVP — проверить ключевую гипотезу:

Люди готовы заполнять профиль, искать компанию для поездки, вступать в direct Chat с потенциальными попутчиками и создавать совместную Trip.

В MVP входит:

- запуск Telegram Mini App;
- вход пользователя через Telegram;
- заполнение профиля;
- анкета путешественника;
- поиск потенциальных попутчиков;
- механика взаимного интереса / мэтча;
- создание чата после совпадения;
- обсуждение поездки;
- Group Chat: создание создателем с минимум тремя участниками, выбором из existing matched/direct-chat companions, persisted messages, отображением в Chats и фиксированным составом;
- создание Trip из existing direct или group Chat через Mini App;
- просмотр сохранённого списка и detail Trip, включая участников и `trip_stops`.

В MVP не входит:

- отдельное мобильное приложение для iOS и Android;
- публикация в App Store и Google Play;
- покупка билетов;
- бронирование отелей;
- полноценная юридическая верификация документов;
- сложная рекомендательная ML-система;
- полноценная геймификация;
- платные подписки;
- маркетплейс украшений профиля;
- интеграции с авиакомпаниями, отелями и туроператорами.
- AI Travel Copilot, `Proposal` и внешние provider-интеграции — до подключения второго разработчика;
- TripInvitation, persisted group title, изменение состава Group Chat после создания и расширенный lifecycle Trip.

Эти функции могут быть добавлены позже, если MVP подтвердит спрос.

## Базовый пользовательский сценарий

1. Пользователь регистрируется.
2. Заполняет профиль: имя, возраст, город, фото, интересы, языки, стиль общения.
3. Заполняет анкету путешественника: бюджет, даты, направления, тип отдыха, уровень комфорта, желаемый формат поездки.
4. Просматривает профили потенциальных попутчиков.
5. Отмечает интересных людей.
6. При взаимном интересе создаётся чат.
7. Участники обсуждают возможную поездку.
8. Backend поддерживает создание Trip из existing direct или group Chat и сразу создаёт TripParticipant для всех активных участников.
9. Mini App вызывает эту команду из Chat Room; при `409` открывает найденную через `GET /trips` unfinished Trip.

## Основные модули продукта

### Профиль пользователя

Профиль нужен не только для знакомства, но и для доверия.

Профиль может включать:

- имя;
- возраст;
- город;
- фото;
- краткое описание;
- интересы;
- любимые форматы отдыха;
- языки;
- ссылки на соцсети;
- бейджи;
- рейтинг;
- отметки верификации.

### Анкета путешественника

Анкета путешественника помогает понять, с кем пользователю будет комфортно путешествовать.

Возможные поля:

- желаемые направления;
- бюджет;
- даты;
- длительность поездки;
- стиль отдыха;
- уровень комфорта;
- отношение к хостелам, отелям и апартаментам;
- отношение к активному отдыху;
- предпочтения по компании;
- важные ограничения.

### Поиск попутчиков

На первом этапе поиск может работать через простые фильтры и скоринг совместимости.

Примеры критериев:

- совпадение бюджета;
- совпадение дат;
- похожие интересы;
- похожий стиль отдыха;
- комфортный возрастной диапазон;
- совпадение направлений;
- похожие ожидания от поездки.

### Чат

Чат — центральное место, где пользователи знакомятся и обсуждают будущую поездку. В текущем MVP direct и group Chat, список, история и создание текстовых сообщений работают через backend; realtime, read state и pagination пока не реализованы.

На старте достаточно:

- личного чата после взаимного интереса;
- группового чата для нескольких участников;
- отправки текстовых сообщений;
- сохранения истории;
- возможности вызвать ИИ-помощника.

### Продуктовая модель Chats / Trips

После завершения логической доменной модели зафиксировано, что:

- `Chats` — живое общение, история обсуждений, system messages, AI-контекст и точка входа в поездки;
- `Trips` — подтверждённое актуальное состояние поездки внутри конкретного чата;
- `Match` создаёт один direct chat 1:1, а исходный direct chat сохраняется даже после появления group chat;
- один chat может содержать несколько последовательных Trip;
- одновременно в одном chat может существовать только одна незавершённая Trip;
- Trip проходит lifecycle `forming → active → completed|cancelled`;
- после перехода Trip в `active` новых участников в неё добавить нельзя;
- подтверждённые блоки `destination`, `dates`, `budget`, `transport` хранятся прямо в `Trip`, а промежуточные варианты живут в `Proposal`;
- AI может готовить summary, предложения и внешние варианты, но не принимает решения за пользователей и не меняет Trip напрямую.

### AI Travel Copilot

AI Travel Copilot остаётся будущим backend-слоем поверх Chats / Trips и ещё не реализован в продукте. В утверждённой архитектуре он работает через `AIRequest`, `ChatSummary`, `Proposal` и внешние provider-интеграции, но не меняет подтверждённое состояние поездки напрямую.

Планируемые функции первой версии:

- суммировать обсуждение и обновлять `ChatSummary`;
- выделять ограничения и несогласованные блоки;
- предлагать направления, даты, бюджет и транспорт через `Proposal`;
- оценивать примерный бюджет и сравнивать варианты;
- находить компромиссы;
- предлагать активности;
- запускать поиск вариантов через backend-интеграции с внешними API.

## Возможные команды ИИ

Примеры команд:

```text
/summary   — суммировать желания участников
/plan      — предложить план поездки
/budget    — оценить примерный бюджет
/cheaper   — сделать поездку дешевле
/activities — предложить активности
/compare   — сравнить несколько направлений
/conflicts — найти противоречия в желаниях участников
/final     — собрать финальный черновик поездки
```

## Предварительный стек

Предлагаемый стек для MVP:

- Клиент: Telegram Mini App
- Фронтенд: Next.js + React + TypeScript
- Интеграция с Telegram: Telegram Web Apps SDK
- Бэкенд: API routes или server actions
- База данных: Supabase Postgres
- Авторизация: серверная проверка Telegram `initData`
- Хранилище: Supabase Storage или совместимое S3-хранилище
- AI-сервис: отдельный backend-слой для LLM и генерации поездок
- LLM: внешний API на старте
- Дизайн: Figma
- Репозиторий: GitHub

Telegram Mini App не отменяет будущую разработку отдельного мобильного приложения. Если MVP подтвердит спрос, текущую продуктовую логику, базу данных и AI-сценарии можно будет переиспользовать для iOS и Android.

## Предварительная структура проекта

```text
travel-friend/
  README.md
  CHANGELOG.md
  docs/
    backend/
      domain-model.md
      er-diagram.md
      backend-architecture.md
      ai-architecture.md
      integrations.md
      security.md
    roadmap.md
    dev-log.md
    team-workflow.md
  apps/
    mini-app/
      app/
      components/
      lib/
```

## Локальный запуск

Команды для работы с текущей Mini App:

```bash
cd apps/mini-app
npm install
npm run dev
npm run typecheck
npm run build
```

## Документация проекта

Основные рабочие документы:

- `CHANGELOG.md` — список значимых изменений;
- `docs/roadmap.md` — ближайшие шаги;
- `docs/dev-log.md` — дневник разработки;
- `docs/team-workflow.md` — правила работы команды.

## Architecture documentation

Backend-документация после завершения логической доменной модели:

- `docs/backend/domain-model.md` — краткая карта доменов, сущностей, lifecycle и инвариантов;
- `docs/backend/er-diagram.md` — утверждённая ER-модель как источник истины;
- `docs/backend/backend-architecture.md` — высокоуровневый backend-контур и зоны ответственности;
- `docs/backend/ai-architecture.md` — модель AIRequest / ChatSummary / Proposal / usage / premium AI;
- `docs/backend/integrations.md` — Telegram, AI Provider и внешние travel-интеграции;
- `docs/backend/security.md` — trusted zones, moderation/safety и security-принципы.

## Ближайшие задачи

Ближайший порядок разработки:

1. MVP mega-review backend-backed direct-chat flow.
2. UI-вызов уже существующего Trip creation API, затем один следующий Trip write/lifecycle slice.
3. Production deployment/RLS/runbook backend.
4. Realtime chat flow после стабилизации HTTP contracts.
5. AI только после подключения второго разработчика.

## Долгосрочное видение

В будущем Travel Friend может стать платформой для групповых путешествий, где пользователи не только ищут попутчиков, но и получают полноценную поддержку на всех этапах поездки:

- поиск компании;
- планирование маршрута;
- бронирование;
- распределение расходов;
- геймификация;
- рейтинги и доверие;
- партнёрские предложения;
- тематические поездки;
- проверенные лидеры поездок;
- B2B-интеграции с travel-компаниями.

Долгосрочная цель — создать социальный слой вокруг путешествий и группового спроса, который может быть интересен крупным travel-tech компаниям.
