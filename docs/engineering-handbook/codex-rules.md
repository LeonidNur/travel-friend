# Правила для Codex

Codex в проекте Travel Friend нужен как исполнитель небольших и чётко очерченных задач. Он не должен принимать продуктовые решения вместо команды, придумывать направление развития продукта или расширять скоуп по собственной инициативе. Его задача — взять понятную задачу и реализовать её аккуратно, предсказуемо и без лишних сюрпризов. Сейчас реализован persisted backend-backed flow: server-side Telegram Auth, onboarding, Profile/TravelIntent, Discover/Match, direct/group Chats с persisted messages и создание/чтение Trip. Production RLS/runtime-role baseline и compatible backend deployment закрыты; realtime, Trip lifecycle/write, moderation, сложный membership lifecycle и AI/provider integrations остаются отдельными задачами.

Codex не определяет текущее состояние проекта по старым записям `CHANGELOG.md` или `docs/dev-log.md`. Для каждой задачи он проверяет релевантный текущий код, tests/migrations и source-of-truth документацию; порядок источников закреплён в `AGENTS.md`. Historical records не могут переопределять current implementation или утверждённые документы.

Перед изменениями Codex сначала показывает короткий план. В этом плане должно быть видно, какие файлы он собирается затронуть и в каком порядке будет работать. Только после этого он вносит правки. Такой порядок нужен не ради формальности, а чтобы команда заранее понимала, что именно изменится.

Когда работа завершена, Codex должен кратко объяснить, что было сделано и почему выбран именно такой путь. Если задача затронула проверки, он запускает `typecheck` и `build`, а если что-то не удалось выполнить, честно сообщает причину. В конце он перечисляет открытые TODO или оставшиеся риски, если они действительно есть.

Если Codex меняет существующий flow, он обязан проверить соседние сценарии, которые могут сломаться или потребовать согласования. Для Travel Friend это особенно важно для уже существующего frontend-flow `Profile → Discover → Chats → Trips` и для соседних backend-сценариев, которые будут опираться на него, но не совпадают с ним по готовности.

Если задача помечена как `docs-only`, Codex не трогает код приложения, не меняет UI-логику и ограничивается документами и документальными проверками.

Если в процессе работы обнаружены новые несостыковки, Codex фиксирует их как TODO или risk, но не исправляет без отдельного разрешения на расширение скоупа.

Есть несколько вещей, которые Codex делать не должен.

- Он не делает изменения «заодно», если они не относятся к задаче.
- Он не добавляет зависимости без явного разрешения.
- Он не переписывает `README.md` без отдельного указания.
- Он не меняет код приложения, если задача помечена как `docs-only`.
- Он не расширяет скоуп работы только потому, что рядом заметил ещё один возможный рефакторинг.
- Он не исправляет новые несостыковки по собственной инициативе, если они были найдены уже в процессе и не входят в исходный скоуп.

Если задача касается только документации, Codex работает только с документами. Код приложения, бизнес-логика и вспомогательные модули при этом остаются нетронутыми. Это важное правило, потому что docs-only задачи должны улучшать знание о проекте, а не менять сам продукт.

Хорошая работа Codex в этом проекте выглядит не как большое количество правок, а как точное попадание в задачу. Чем меньше лишнего движения вокруг, тем выше качество результата.
